>core下面计划会放置核心业务逻辑，预期由
>>1.router:路由相关，两部分，一个是监听服务端口，接收新的链接请求。接下来是核心part，接收请求后，创建新的goroutine去处理请求。这个显然是需要有状态的，且考虑到我们的接口存在收发两端，并且收发场景可能同时出现（比如A、B同时发送消息，对A入口来说，就会出现A传来一个消息，需要对其进行回复，同时也需要转发一个B的消息到A）。为此我想到的方式是去开辟多个队列，[serv->clnt(req + resp)] + [clnt->serv(req + resp)], 这样每个队列都是串行的，我们只需要根据场景去决定要做哪些操作
>
>>2.model:数据模型相关,提供一些全局的模型维护，需要考虑清楚哪些数据存在并发场景，上方已经说明了一部分model应该有的样子，主要由几个part组成：
>
>>3.intf:这里主要是想说是不是抽象一些接口，用来操作model，这些接口应该和对外暴露的动作一一对应
